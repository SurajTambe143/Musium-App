<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/color_Accent</item>
    </style>

    <style name="Theme.MusicApp" parent="Base.Theme.MusicApp" />
    <style name="corner_radius" parent="Base.Theme.MusicApp">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10%</item>
    </style>

    <style name="bottom_corner" parent="Base.Theme.MusicApp">
        <item name="cornerSizeBottomLeft">80dp</item>
        <item name="cornerFamilyBottomLeft"> rounded</item>
        <item name="cornerSizeBottomRight">80dp</item>
        <item name="cornerFamilyBottomRight"> rounded</item>
    </style>

</resources>